<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fabanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FabAnimation">
        <activity
            android:name=".DrawerBottomNavigationActivity"
            android:exported="true"
            android:label="@string/title_activity_drawer_bottom_navigation"
            android:theme="@style/Theme.FabAnimation.NoActionBar">
        </activity>
        <activity android:name=".input.OtherActivity" />
        <activity android:name=".InputActivity"></activity>
        <activity android:name=".MonthActivity"></activity>
        <activity android:name=".selectable.ExampleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>